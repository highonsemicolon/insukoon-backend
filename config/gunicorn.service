[Unit]
Description=gunicorn daemon
Requires=gunicorn.socket
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/insukoon-backend
ExecStart=/home/ubuntu/insukoon-backend/env/bin/gunicorn \
          --access-logfile - \
          --workers 3 \
          --bind unix:/run/gunicorn.sock \
          insukoon.wsgi:application
[Install]
WantedBy=multi-user.target
